import 'package:flutter/material.dart';

/// Class [Palette] to handle colors of the app.
class Palette {
  /// Background [Color]
  static const background = Color(0xfff4f7ff);

  /// Grey [Color]
  static const grey = Color(0xffb6c1da);

  /// Black [Color]
  static const black = Color(0xff334576);

  /// Purple [Color]
  static const purple = Color(0xff5b1bef);

  /// PurpleLight [Color]
  static const purpleLight = Color(0xff652ef3);

  /// backgroundOnboarding [Color]
  static const backgroundOnboarding = Color(0xffebf3ff);
}
